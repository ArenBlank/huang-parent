server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fitness_platform?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=GMT%2b8
    username: root
    password: root
  data:
    redis:
      host: localhost
      port: 6379
      password: root
      database: 0

minio:
  endpoint: http://files.localhost
  public-endpoint: http://files.localhost
  access-key: root
  secret-key: rootroot
  bucket-name: lease
